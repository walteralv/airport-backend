<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Ticket de Reserva</title>
    <style>
        body { font-family: Arial, sans-serif; }
        .ticket { border: 1px solid #000; padding: 20px; max-width: 600px; margin: 0 auto; }
        .header { text-align: center; border-bottom: 1px solid #000; padding-bottom: 10px; }
        .details { margin-top: 20px; }
        .passenger { margin-bottom: 10px; }
    </style>
</head>
<body>
    <div class="ticket">
        <div class="header">
            <h1>Ticket de Reserva</h1>
            <p>Reserva ID: {{bookingId}}</p>
        </div>
        <div class="details">
            <h2>Detalles del Vuelo</h2>
            <p>Número de Vuelo: {{flight.flightNumber}}</p>
            <p>Origen: {{flight.origin}}</p>
            <p>Destino: {{flight.destination}}</p>
            <p>Salida: {{flight.departureTime}}</p>
            <p>Llegada: {{flight.arrivalTime}}</p>
            
            <h2>Pasajeros</h2>
            {{#each passengers}}
            <div class="passenger">
                <p>Nombre: {{this.firstName}} {{this.lastName}}</p>
                <p>Pasaporte: {{this.passportNumber}}</p>
                <p>Asiento: {{this.seat}}</p>
                <p>Equipaje: {{this.baggage}}</p>
            </div>
            {{/each}}
            
            <h2>Resumen de Pago</h2>
            <p>Precio Total: {{totalPrice}}</p>
            <p>Estado: {{status}}</p>
            <p>Fecha de Reserva: {{bookingDate}}</p>
        </div>
    </div>
</body>
</html>